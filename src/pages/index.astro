---
import BaseLayout from "@/layouts/BaseLayout.astro"
import HeroSection from "@/components/HeroSection"
const items = new Array(100).fill('hello world!')
---

<BaseLayout>
	<HeroSection client:load />
	{items.map(item => <h1>{item}</h1>)}
</BaseLayout>